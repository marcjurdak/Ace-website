# ACE website

- `index.html` is the shop.
- `admin/index.html` is the admin screen (open yoursite.com/admin).
- `config.js` holds the Supabase address and public key.
- `images/` holds the starting product photos. New photos you upload in the admin are stored in Supabase.
